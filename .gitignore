# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Server-managed files (Hostinger) ──────────────────────────────────────────
# These files must be uploaded/maintained manually on the server after each deploy.
# They are intentionally NOT tracked by git so that git pull / CI deploys
# never overwrite them.  See public/PROTECTED_FILES.md for the full list.
public/.htaccess
public/*.html
